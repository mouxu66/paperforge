# PaperForge requirements.lock — 自动生成
# 原始文件: mock_api\requirements.txt
# 生成命令: python scripts\lock_requirements.py

fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
pydantic-settings==2.14.2
SQLAlchemy==2.0.44
requests==2.32.5
zhipuai==2.1.5.20250825
python-docx==1.2.0
pypdf==6.14.2
python-multipart==0.0.32
apscheduler==3.11.2

# 仅打包教师版 .exe 时需要（开发模式可不装）
pyinstaller==6.21.0

# ---- 可选：向量语义搜索（混合检索 FTS5 + 向量 + RRF 融合）----
# 安装：pip install fastembed>=0.2.0 numpy>=1.24.0
# 首次使用需运行 python -m scripts.embed_papers 生成向量（会自动下载 ~130MB 模型）
# 模型缓存路径：~/.cache/fastembed/（BAAI/bge-small-en-v1.5，384 维，纯 CPU 推理）
# 教师版打包不含此依赖与模型文件，未联网时自动降级为 FTS5 关键词检索
fastembed==0.8.0
numpy==2.3.5

# ---- 开发依赖（测试用）----
# 安装：pip install -r mock_api/requirements.txt
# 运行测试：pytest tests/ -v
pytest==8.4.2
pytest-cov==7.1.0
pytest-asyncio==1.4.0
httpx==0.28.1
